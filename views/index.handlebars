<h1>Burgers!</h1>

<h2>Burgers that are not Devour!</h2>

<ul>
  {{#each brgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devourey!</h2>

<ul>
  {{#each burgers}}
    {{#if devourey}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="bdevouy">devourey?</label><br>
    <input type="radio" name="devourey" value="1" checked> devourey!<br>
    <input type="radio" name="devourey" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>